# Diamond-price-prediction
Machine learning model for predicting diamond prices based on physical attributes  (carat, cut, color, clarity, etc.). Includes Flask web application, advanced regression  algorithms (XGBoost, LightGBM), and Docker deployment support.
